project_id = "blhidceerkrumgxjhidq"

[functions.route-ticket-email]
verify_jwt = true  # SECURITY: Require authentication to prevent email spam/XSS

[functions.register-remote-client]
verify_jwt = true  # SECURITY: Prevent unauthorized client registration

[functions.send-staff-onboarding-email]
verify_jwt = true  # SECURITY: Prevent email spam

[functions.resend-provider-email]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.confirm-provider-task]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.storage-admin-operations]
verify_jwt = true  # SECURITY: Require authentication for admin operations

[functions.check-migrations]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.manage-user-roles]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.import-staff-users]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.notify-ticket-assignment]
verify_jwt = true  # SECURITY: Require authentication to prevent email spam

[functions.send-ticket-reminders]
verify_jwt = true  # SECURITY: Require authentication to prevent email abuse

[functions.reset-user-password]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.sync-microsoft-365]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.m365-ediscovery-search]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.verify-deployment]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.mark-migrations-applied]
verify_jwt = false  # NOTE: JWT verification is handled inside the function to allow CORS preflight requests

[functions.sync-microsoft-365-debug]
verify_jwt = false  # NOTE: Debug function - no JWT required for diagnostic token checks